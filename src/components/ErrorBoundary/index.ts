/**
 * Error Boundary Component Exports
 * 
 * Barrel file for exporting the ErrorBoundary component.
 * The ErrorBoundary is a class component that catches errors
 * in its child component tree and displays a fallback UI.
 * 
 * @module Components/ErrorBoundary
 */

export { ErrorBoundary } from './ErrorBoundary'; 